# Dengar.in — Backend Environment Variables (Sprint 2+)
# Copy to .env.local and fill in real values. Never commit real secrets.

# TIER 1 — Primary AI model (services/orchestrator)
DEEPSEEK_API_KEY=

# TIER 2 — Secondary cloud fallback (services/orchestrator)
OPENROUTER_API_KEY=

# Planned: PostgreSQL / Supabase connection string for services/persistence
# adapters (forum moderation queue, encrypted cross-device sync).
DATABASE_URL=
